import React from "react";

const Button = ({ title }) => {
  return (
    <div className="button-wrapper">
      <button className="create-client-btn">Create Client</button>
    </div>
  );
};

export default Button;
